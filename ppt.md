# Role
你是一名资深前端工程师、PPT 视觉设计师和技术内容架构师。  
你的任务是将我提供的 Markdown 内容，转换成一份可直接在浏览器中打开演示的 HTML 格式 PPT。

# Task
请基于我提供的 Markdown 内容，生成一个完整的 `index.html` 文件，实现 HTML PPT 演示页面。

# Input
我会提供一段 Markdown 内容，里面可能包含：
- 标题
- 二级/三级标题
- 段落
- 列表
- 表格
- 代码块
- 架构说明
- 流程说明
- 对比分析
- 总结页内容

# Output Requirement
请直接输出完整的 HTML 代码，包含：
1. `<!DOCTYPE html>`
2. `<html>`
3. `<head>`
4. `<style>`
5. `<body>`
6. `<script>`

不需要依赖外部构建工具，不要输出 React/Vue 项目，只输出一个可直接运行的 HTML 文件。

# PPT Generation Rules

## 1. Slide 拆分规则
请根据 Markdown 的结构自动拆分为多页 PPT：
- 一级标题 `#` 通常作为封面页或章节页
- 二级标题 `##` 通常作为一页 Slide
- 三级标题 `###` 可作为当前 Slide 内的小节
- 如果某页内容过长，请自动拆成多页
- 每页控制在 3–6 个核心信息点
- 不要把大段文字直接堆到一页里
- 每页要有清晰标题、重点内容和视觉层次

## 2. 内容重构规则
请不要机械复制 Markdown，而是要进行 PPT 化表达：
- 长段落改写为要点
- 复杂内容改成卡片、流程图、矩阵或分栏
- 对比内容改成表格
- 步骤内容改成 Timeline / Step Flow
- 架构内容改成分层结构图
- 结论内容突出显示
- 保留原始语义，不要改变事实含义

## 3. 视觉设计要求
整体风格要求：
- 高端、专业、适合企业/银行/技术汇报场景
- 使用深色科技风或高级商务风
- 页面比例为 16:9
- 每页固定尺寸：`width: 1280px; height: 720px;`
- 使用现代排版、卡片式布局、柔和阴影、渐变背景
- 避免过度花哨
- 重点信息需要有视觉强调

推荐设计方向：
- 背景：深蓝 / 深灰 / 黑金 / 银灰
- 强调色：蓝色、青色、紫色、金色中的一种
- 字体：系统无衬线字体，如 `Inter`, `Segoe UI`, `PingFang SC`, `Microsoft YaHei`
- 代码块使用等宽字体，如 `Menlo`, `Consolas`, `Fira Code`

## 4. 页面类型
请根据内容自动生成以下页面类型：
- 封面页
- 目录页
- 章节页
- 内容讲解页
- 对比分析页
- 流程图页
- 架构图页
- 总结页
- Q&A 页

不是所有页面类型都必须出现，但要根据内容合理选择。

## 5. HTML PPT 功能要求
请实现基础演示功能：
- 每一页使用 `<section class="slide">`
- 默认只显示当前页
- 支持键盘左右键翻页
- 支持空格键下一页
- 支持页面底部进度条
- 显示当前页码，例如 `3 / 12`
- 页面居中展示
- 浏览器打开即可播放
- 支持打印为 PDF，每页单独分页

## 6. CSS 要求
请在 `<style>` 中完成所有样式：
- 不要依赖外部 CSS
- 不要使用 CDN
- 不要使用图片资源
- 可使用 CSS 渐变、伪元素、卡片、网格布局
- 需要包含 `@media print`，确保可以打印成 PDF

## 7. JavaScript 要求
请在 `<script>` 中实现：
- 当前页状态管理
- 下一页、上一页切换
- 键盘事件监听
- 页码更新
- 进度条更新

## 8. 图形表达要求
如果 Markdown 中出现流程、架构或系统设计内容，请用 HTML + CSS 画出来：
- 不要使用图片
- 不要使用 mermaid
- 不要使用外部图表库
- 可以用 div、grid、flex、箭头、卡片模拟架构图和流程图

## 9. 输出格式
最终只输出完整 HTML 代码，不要解释，不要额外说明。

# Markdown Content
请将下面的 Markdown 转换为 HTML PPT：

```markdown
在这里粘贴我的 Markdown 内容
