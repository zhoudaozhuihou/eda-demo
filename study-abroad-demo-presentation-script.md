# 留学金融标签全流程演示逐字稿

## 1. 文档用途

本文档用于产品评审现场演示“AI 智能标签平台”的完整用户旅程。演示主要在智能工作台中完成，通过自然语言对话依次展示：

1. 业务需求理解
2. 标签需求拆解
3. 已有标签检索与复用
4. 缺失标签识别
5. 知识与数据证据匹配
6. 标签规则开发、验证与发布
7. 目标客群圈选与画像分析
8. 数据质量、合规与审计检查

完整技术说明见 [产品技术架构说明](./product-technical-overview.md)。

## 2. 演示目标

通过一个“留学金融机会发现”场景，向评审人员证明平台能够将一句业务需求转化为可复用、可验证、可发布、可审计的标签资产，并最终形成可用于营销的目标客群。

本次演示需要让评审人员形成以下认知：

- 业务人员不需要先理解数据表和字段，只需要描述业务目标。
- AI 会优先复用已有标签，不会重复建设。
- 缺失标签必须由知识和数据证据共同支撑。
- 标签不是生成后直接使用，而是经过规则配置、样本验证、审批和发布。
- 圈选结果能够解释命中原因、客群画像、数据质量和合规依据。
- 整个过程留有任务记录、版本记录、数据绑定和审计记录。

## 3. 演示方式

### 3.1 推荐方式

使用 SQL 中预置的历史会话进行稳定演示：

- 会话名称：`留学金融机会发现完整流程`
- 会话模式：`demo`
- 会话消息数：12
- 工作台运行记录数：4

演示时选择该会话，按照本文档顺序滚动并讲解。这样可以避免现场模型响应时间、网络状态和生成结果波动。

### 3.2 现场交互方式

如需体现真实交互，可新建一个演示会话，并严格按照本文档中的“Chat 输入”逐条输入。建议在正式评审前完整彩排一次，确认返回数据与预置数据一致。

### 3.3 建议时长

| 环节 | 建议时长 |
| --- | ---: |
| 开场与场景说明 | 1 分钟 |
| 需求理解与标签发现 | 2 分钟 |
| 知识与证据匹配 | 2 分钟 |
| 缺失标签开发 | 3 分钟 |
| 客群圈选与画像 | 2 分钟 |
| 质量、合规与技术说明 | 2 分钟 |
| 总计 | 12 分钟 |

## 4. 演示前准备

### 4.1 导入演示数据

```bash
docker compose up -d

docker compose exec -T postgres psql -U user -d data_label \
  < backend/sql/seed_study_abroad_demo_journey.sql
```

该 SQL 可重复执行。执行前需要先启动后端，使应用完成数据库表结构初始化。

### 4.2 打开系统

- Docker 部署地址：`http://localhost`
- 前端开发地址：`http://localhost:5173`
- 工作台路由：`/studio`

### 4.3 演示前检查

- 确认后端健康检查正常。
- 确认系统显示“演示数据”模式。
- 确认左侧会话列表中存在“留学金融机会发现完整流程”。
- 确认该会话未被归档或清空。
- 确认工作台能够看到完整的 12 条对话消息。
- 浏览器缩放建议保持在 90% 至 100%。
- 演示期间不要切换到“标签分析”页面重新执行通用演示圈选，以免出现与本场景不同的通用演示数字。

## 5. 完整演示逐字稿

## 5.1 开场：从业务问题开始

### 页面操作

1. 进入“智能工作台”。
2. 在左侧会话列表选择“留学金融机会发现完整流程”。
3. 将页面滚动到第一条用户消息。

### 主持人演讲词

> 今天演示的是一个留学金融机会发现的完整场景。
>
> 假设零售银行希望找到家里有子女在海外留学、同时存在跨境金融需求的客户，向这些客户推荐留学汇款、外汇预约和国际信用卡等产品。
>
> 传统做法通常需要业务人员先找数据团队，再由数据团队查表、找字段、写规则、核对授权，整个过程会跨越多个系统。我们的产品希望把这条链路收敛到一个智能工作台中。
>
> 业务人员不需要先知道应该使用哪些表或哪些字段，只需要描述业务目标。

### Chat 输入 1

```text
我们想找出有子女在国外留学的客户，向他们推荐留学汇款产品。请帮我看看现有标签够不够。
```

### 预期系统回复

> 我已把需求拆成四类标签要素：家庭关系里的“有留学子女”、行为意向里的“有留学需求”、交易特征里的“留学汇款/跨境汇款活跃”，以及营销名单必须具备的“营销可触达”。前三类在标签市场中已有可复用标签，“营销可触达”和最终客群标签“留学金融潜客”需要开发并发布。

### 指屏重点

- 指出 AI 将自然语言拆成了四个业务维度。
- 指出系统同时给出了“已有标签”和“待开发标签”。
- 指出工作台不是只回答问题，而是在组织后续执行步骤。

### 衔接演讲词

> 这里最重要的不是 AI 给出了几个名词，而是它先形成了标签需求清单，并且开始判断哪些资产可以复用、哪些能力还存在缺口。
>
> 接下来我们让系统检索标签市场，确认这些判断。

## 5.2 标签发现：已有资产优先复用

### Chat 输入 2

```text
先查一下留学标签有哪些
```

### 预期系统回复

> 标签市场中已匹配到 7 个留学相关标签：有留学需求、有留学子女、留学汇款、跨境汇款活跃、有外汇需求、跨境消费、高净值客户。它们可以覆盖需求识别、家庭证据和交易行为，但缺少“营销可触达”这个合规准入标签，因此不能直接导出营销名单。

### 工作台预期结果

工作台显示“标签发现：留学”的执行结果，过程包括：

1. 解析标签需求
2. 检索标签市场
3. 整理候选标签

预期标签结果：

| 标签 | 类型 | 覆盖人数 | 置信度 | 当时状态 |
| --- | --- | ---: | ---: | --- |
| 有留学需求 | 行为意向 | 18,640 | 96 | 已发布 |
| 有留学子女 | 家庭关系 | 15,820 | 94 | 已发布 |
| 留学汇款 | 交易特征 | 4,960 | 95 | 已发布 |
| 跨境汇款活跃 | 交易特征 | 22,800 | 88 | 已发布 |
| 营销可触达 | 合规授权 | 842,600 | 99 | 待开发 |
| 留学金融潜客 | 客群标签 | 12,480 | 92 | 待开发 |

### 主持人演讲词

> 系统已经找到了七个可以复用的留学相关标签。它们分别覆盖家庭关系、留学意向、交易行为、外汇需求和资产等级。
>
> 这体现了平台的第一个设计原则：复用优先。AI 不会因为来了一个新需求，就重新开发一套含义相同的标签。
>
> 但系统也发现了一个关键问题。我们虽然能判断谁可能有留学金融需求，却还不能证明这些客户允许被营销触达。如果缺少营销授权校验，名单就不能直接导出。
>
> 因此，这次真正需要补齐的是“营销可触达”，以及将多个基础标签组合起来的“留学金融潜客”。

### 指屏重点

- 已发布标签与待开发标签的状态差异。
- 标签类型、覆盖人数和置信度。
- “营销可触达”是业务名单进入营销环节的合规门槛。

### 衔接演讲词

> 下一步不是让 AI 自己编一条规则，而是先寻找企业知识和数据证据，确认“营销可触达”在业务上到底应该如何定义。

## 5.3 知识获取：从制度和产品知识中提取标签依据

### Chat 输入 3

```text
缺少的标签怎么开发？能不能结合知识库？
```

### 预期系统回复

> 可以。知识库里已经有“营销授权与客户触达合规规则”，明确了营销可触达的三条规则：授权状态为 granted、至少一个渠道允许触达、授权有效期覆盖活动周期。我会把这条知识和 marketing_consent 表的 consent_status、channel_allowed、valid_until 字段作为证据，进入标签开发流程。

### 主持人演讲词

> 可以看到，工作台开始调用知识能力。
>
> 平台从“营销授权与客户触达合规规则”中提取出了三条明确要求：第一，授权状态必须是 granted；第二，App、短信或者电话至少有一个渠道允许触达；第三，授权有效期必须覆盖本次活动周期。
>
> 同时，平台没有停留在文档语义层面，而是继续把知识规则映射到了实际数据资产，包括 marketing_consent 表，以及 consent_status、channel_allowed、valid_until 三个字段。
>
> 这一步建立的是“知识定义”和“数据证据”之间的关系。后续标签规则中的每个条件都能够解释它来自哪份制度、哪张表、哪个字段。

### 工作台关联知识

本次场景使用 5 份知识文档：

1. `留学金融综合服务方案`
2. `跨境留学汇款政策要点`
3. `境外教育缴费产品说明`
4. `留学信用卡与外汇服务说明`
5. `营销授权与客户触达合规规则`

知识抽取内容包括：

- 候选标签
- 业务实体
- 实体关系三元组
- 文档摘要
- 标签置信度与生成原因

### 指屏重点

- 知识文档名称。
- 三条营销授权规则。
- 知识规则到表字段的映射。
- 系统给出的下一步建议：查看知识证据、生成规则草案、执行样本验证。

### 衔接演讲词

> 现在，业务定义和数据证据都已经具备，我们可以正式启动缺失标签的开发。

## 5.4 标签开发：定义、证据、规则、验证、发布

### Chat 输入 4

```text
请开发营销可触达和留学金融潜客
```

### 预期系统回复

> 已完成两个标签开发任务。“营销可触达”命中 842,600 人，覆盖率 65.8%，合规样本已审批；“留学金融潜客”由留学需求、海外子女、营销授权和资产/外汇信号组合生成，验证命中 12,480 人，覆盖率 3.2%，已发布到标签市场并绑定回相关数据表。

### 工作台预期执行步骤

1. 读取知识证据
2. 选择数据证据
3. 执行样本验证
4. 发布新标签

### 主持人演讲词

> 工作台现在启动了两个标签开发任务。后台实际遵循的是一套五步标签开发流程：定义标签目标、选择数据证据、配置标签规则、执行样本验证、确认发布。
>
> 第一个标签是“营销可触达”。它的业务口径是：客户授权状态为 granted，至少有一个允许触达的渠道，并且授权有效期不早于活动结束日期。
>
> 第二个标签是“留学金融潜客”。它不是一个单字段标签，而是由已有标签和客户数据组合生成的客群标签。

### “营销可触达”规则

```text
consent_status = granted
AND channel_allowed IN (app, sms, phone)
AND valid_until >= 2026-12-31
```

### “留学金融潜客”规则

```text
study_abroad_intent_score >= 70
AND has_overseas_child = true
AND marketing_contactable = true
AND aum_balance >= 500000
```

### 继续演讲词

> 规则生成后，系统会进行样本验证，而不是直接发布。
>
> “营销可触达”命中 842,600 人，覆盖率 65.8%。验证过程检查了授权正常客户，也专门检查了授权撤回、授权过期和仅允许服务通知的反例，最终由合规角色完成审批。
>
> “留学金融潜客”命中 12,480 人，覆盖率为 3.2%。样本中既包含高匹配客户，也包含未命中客户和边界客户。例如，留学意向分刚好达到 70 分的客户会被列入边界样本，供业务人员继续观察。
>
> 两个标签验证通过后发布为 v1，并自动回流到标签市场，同时建立与知识、表和字段的绑定关系。后续其他业务场景可以直接复用。

### 核心结果

| 标签 | 命中人数 | 覆盖率 | 审批结果 | 发布状态 |
| --- | ---: | ---: | --- | --- |
| 营销可触达 | 842,600 | 65.8% | 合规审批通过 | v1 已发布 |
| 留学金融潜客 | 12,480 | 3.2% | 样本验证通过 | v1 已发布 |

### 指屏重点

- 工作台任务进度为 100%。
- 规则条件是结构化条件，不是自然语言备注。
- 验证过程包含命中、未命中和边界样本。
- 发布结果包含版本和数据绑定。

### 衔接演讲词

> 到这里，我们已经把两个缺失标签补齐，并沉淀成正式资产。接下来业务人员仍然不需要离开工作台，可以直接使用这些标签圈选目标客户。

## 5.5 客群圈选：从标签资产到可运营名单

### Chat 输入 5

```text
发布后用这些标签圈选目标客户
```

### 预期系统回复

> 已执行圈选：留学金融潜客 = 是，且营销可触达 = 是，命中 12,480 位客户，覆盖全部零售客户的 3.2%。样本中高匹配客户主要集中在 36-45 岁、财富/私行层级，近期有境外教育缴费、留学咨询或外汇预约行为。

### 圈选条件

```text
留学金融潜客 = 是
AND 营销可触达 = 是
```

### 主持人演讲词

> 标签发布以后，业务人员可以继续在工作台中直接发起客群圈选。
>
> 本次圈选要求客户同时满足“留学金融潜客”和“营销可触达”。最终命中 12,480 位客户，占全部零售客户的 3.2%。
>
> 工作台除了返回人数，还会展示客群画像和样本解释。高匹配客户主要集中在 36 到 45 岁、财富和私行层级，近期通常出现过境外教育缴费、留学咨询或者外汇预约行为。
>
> 这使得圈选结果不再只是一个无法解释的客户 ID 列表。业务人员可以知道客户为什么被选中，也可以进一步保存客群、导出经过授权校验的名单，或者用于后续营销策略分析。

### 预期画像结果

- 命中客户：12,480
- 覆盖率：3.2%
- 核心年龄段：36 至 45 岁，占 42%
- 主要资产区间：100 万至 600 万
- 主要客户层级：财富、私行
- 典型行为：境外教育缴费、留学咨询、外汇预约

### 样本客户

| 客户 | 标签数 | 匹配分 |
| --- | ---: | ---: |
| 张伟 | 6 | 97 |
| 李芳 | 5 | 92 |
| 王强 | 5 | 88 |
| 陈静 | 4 | 81 |
| 刘洋 | 4 | 76 |

### 指屏重点

- 命中人数与覆盖率。
- 样本客户及匹配分。
- 年龄、资产和风险偏好分布。
- “保存为客群”“导出样本名单”“查看数据质量”等后续建议。

### 衔接演讲词

> 在名单真正进入营销环节之前，我们还需要回答最后一个问题：这个结果的数据质量是否可靠，合规证据是否完整。

## 5.6 质量与合规：让结果可以被审计

### Chat 输入 6

```text
数据质量和合规证据怎么样？
```

### 预期系统回复

> 本次圈选使用 7 张核心表和 5 份知识证据，数据价值评分 86。关键字段空值率均低于 12%，唯一需要关注的是家庭教育表的境外院校国家字段空值率 18%，但它不作为硬性圈选条件。营销授权规则已通过合规审批，授权 revoked、expired 或仅服务通知客户已被排除。

### 主持人演讲词

> 工作台对本次结果继续执行了数据价值与治理检查。
>
> 整个场景使用了 7 张核心表、16 个证据字段和 5 份知识文档，综合数据价值评分为 86。
>
> 关键圈选字段的空值率都在可接受范围内。当前发现一个需要治理的字段：家庭教育表中的境外院校国家字段空值率为 18%。系统明确指出它需要改进，但因为该字段不属于本次硬性圈选条件，所以不会阻断当前结果。
>
> 在合规方面，营销授权规则已经完成审批。授权状态为 revoked、授权已过期或者只允许服务通知的客户，都已经被排除在名单之外。
>
> 这一步的价值是让业务人员不仅拿到结果，还能同时拿到数据质量说明、知识依据、审批结论和审计记录。

### 工作台预期指标

| 指标 | 结果 |
| --- | ---: |
| 留学金融潜客 | 12,480 |
| 已发布相关标签 | 13 |
| 核心证据表 | 7 |
| 证据字段 | 16 |
| 知识文档 | 5 |
| 待治理字段 | 1 |
| 数据价值评分 | 86 |

### 指屏重点

- 数据价值评分 86。
- 关键字段质量与待治理字段。
- 合规审批结果。
- 审计、治理和执行记录可以继续追踪。

## 5.7 技术口播

### 主持人演讲词

> 最后用一分钟介绍一下这套能力背后的技术实现。
>
> 前端采用 React、TypeScript、Vite 和 Material UI，工作台结果通过结构化组件展示标签、规则、进度和画像，不是把所有内容都当成纯文本聊天。
>
> 后端采用 FastAPI、Pydantic 和 SQLAlchemy，工作台每次请求都会保留会话上下文，调用 Agent 进行需求理解，再根据意图创建标签检索、标签开发、客群圈选或者治理检查任务。
>
> 对于耗时操作，系统使用 Workbench Run 保存执行状态、步骤、进度和结构化结果，前端可以持续获取最新状态。
>
> 知识模块会从文档中提取候选标签、实体和关系三元组，再把知识规则映射到真实表和字段。标签开发过程采用定义、证据、规则、验证、发布五步状态机，并通过输入签名判断验证结果是否因为规则或证据变化而失效。
>
> 数据层使用 PostgreSQL，当前 Docker 环境采用 PostgreSQL 16 和 pgvector 镜像。标签定义、版本、证据、绑定、验证、发布、会话和审计记录都会持久化保存。
>
> 模型层通过统一接口接入兼容 OpenAI 协议的模型服务，当前代码支持 DeepSeek 和 GitHub Copilot Provider。演示模式和正式 AI 模式相互隔离，因此产品评审可以稳定复现，正式环境也可以替换模型和真实数据源。

## 5.8 总结收尾

### 主持人演讲词

> 以上就是留学金融机会发现的完整流程。
>
> 我们从一句业务需求开始，先识别并复用了七个已有标签，再通过知识和数据证据补齐“营销可触达”和“留学金融潜客”两个缺失标签。
>
> 新标签完成了规则配置、样本验证、合规审批和版本发布，随后直接用于圈选 12,480 位目标客户，并完成画像、数据质量和合规检查。
>
> 整个过程中，业务人员主要在一个工作台里通过自然语言完成操作，但后台形成的不是一次性聊天结果，而是一套可复用、可解释、可验证、可版本化、可审计的数据资产。

## 6. Chat 输入速查

正式演示时，按以下顺序输入：

### 第 1 轮

```text
我们想找出有子女在国外留学的客户，向他们推荐留学汇款产品。请帮我看看现有标签够不够。
```

### 第 2 轮

```text
先查一下留学标签有哪些
```

### 第 3 轮

```text
缺少的标签怎么开发？能不能结合知识库？
```

### 第 4 轮

```text
请开发营销可触达和留学金融潜客
```

### 第 5 轮

```text
发布后用这些标签圈选目标客户
```

### 第 6 轮

```text
数据质量和合规证据怎么样？
```

## 7. 核心数字速查

| 项目 | 数值 |
| --- | ---: |
| 初始可复用标签 | 7 |
| 本次开发标签 | 2 |
| 最终标签包标签数 | 6 |
| 营销可触达命中人数 | 842,600 |
| 营销可触达覆盖率 | 65.8% |
| 留学金融潜客命中人数 | 12,480 |
| 留学金融潜客覆盖率 | 3.2% |
| 核心数据表 | 7 |
| 证据字段 | 16 |
| 知识文档 | 5 |
| 数据价值评分 | 86 |
| 待治理字段 | 1 |

## 8. 现场问题与回答

### 为什么不直接让 AI 生成客户名单？

> 因为客户名单涉及业务口径、数据质量和营销合规。平台要求 AI 先形成标签定义，再绑定知识和数据证据，经过规则验证和审批后才能发布和圈选。

### 如果现有标签已经存在怎么办？

> 系统会优先检索标签市场并提示复用。只有不存在或者业务口径不满足要求的标签才进入开发流程。

### 知识库在流程中的作用是什么？

> 知识库负责提供业务定义、制度要求、产品规则和实体关系。系统会将知识中的规则映射到真实数据表和字段，形成可追溯证据。

### AI 生成的规则是否可以修改？

> 可以。AI 生成的是规则草案，开发人员可以调整字段、运算符、阈值和组合关系。规则发生变化后，已有验证结果会被识别为可能失效，需要重新验证。

### 如何保证名单合规？

> 圈选规则强制包含“营销可触达”，并检查授权状态、允许渠道和授权有效期。授权撤回、过期或只允许服务通知的客户会被排除。

### 是否支持真实数据和其他模型？

> 支持。演示模式用于稳定展示，正式模式可以连接真实数据库，并通过统一模型接口切换兼容的模型服务。完整说明见技术文档。

## 9. 异常兜底

### 工作台没有自动选中预置会话

在左侧会话列表手动选择“留学金融机会发现完整流程”。

### 工作台返回结果较慢

等待运行卡片完成，不要连续重复发送相同指令。若现场网络或模型服务不稳定，立即切换回预置会话。

### 返回数字与逐字稿不一致

以预置会话中的结果为准：

- 营销可触达：842,600，覆盖率 65.8%
- 留学金融潜客：12,480，覆盖率 3.2%

不要在“标签分析”页面重新运行通用演示圈选。

### 页面误跳转到其他模块

返回 `/studio`，重新选择预置会话。主流程无需依赖其他模块页面完成。

### 模型服务不可用

说明当前演示采用预置可复现数据，继续展示历史会话和结构化执行结果。技术上，演示模式与正式模型调用相互隔离。
