# 银行智能标签平台 — 产品需求文档 (PRD)

**版本:** v1.0
**日期:** 2026-06-25
**状态:** Draft
**作者:** Product Team

---

## 目录

1. [执行摘要](#1-执行摘要)
2. [背景与问题](#2-背景与问题)
3. [目标用户与角色](#3-目标用户与角色)
4. [用户痛点分析](#4-用户痛点分析)
5. [产品愿景](#5-产品愿景)
6. [战略设计](#6-战略设计)
7. [功能需求](#7-功能需求)
8. [用户旅程](#8-用户旅程)
9. [成功指标](#9-成功指标)
10. [发布策略](#10-发布策略)
11. [附录](#11-附录)

---

## 1. 执行摘要

银行标签平台是一个面向数据部门的企业级工具，解决标签从"业务需求 → 技术开发 → 验证发布 → 复用治理"全生命周期的管理问题。

在传统银行数据环境中，标签开发涉及多个角色（IT BA、数据工程师、业务分析师）和多个系统（数据仓库、SQL 编辑器、Excel、邮件审批），导致一个标签从提出到上线平均耗时 5-10 个工作日。本平台通过 **5 步引导式工作流** + **AI 辅助规则生成** + **标签资产市场**，将单标签开发周期缩短至 1-2 小时，同时确保每个标签的血缘可追溯、验证可复现、资产可复用。

**核心指标目标：**
- 新用户首次完成标签开发时间 ≤ 15 分钟
- 标签开发周期从 5-10 天 → 1-2 小时
- 标签复用率 ≥ 40%（新标签开发时先查市场）
- 已发布标签 100% 可追溯到证据表/字段

---

## 2. 背景与问题

### 2.1 行业背景

银行数据部门面临"数据多、标签少、治理难"的困境：

- **数据量大**：一家中型银行有 500+ 张业务表、10,000+ 个字段
- **标签需求多**：营销、风控、合规各部门每周提出 10-20 个新标签需求
- **治理要求严**：银保监会数据治理指引要求标签可追溯、可审计

### 2.2 当前工作流

```
业务提出需求 → IT BA 写需求文档 → 数据工程师找数据 → 写 SQL/T+1 跑数
→ 验证 → 人工审核 → 发布到下游系统 → 文档归档 → 结束
```

每个环节 0.5-2 天，流转 5-10 天完成一个标签。

### 2.3 核心差距

| 维度 | 当前状态 | 目标状态 |
|------|---------|---------|
| 需求理解 | 自然语言 → IT BA 人工翻译 → 技术规则 | AI 辅助解析，自动生成草稿 |
| 数据发现 | 查数据字典/问 DBA | 已连接资产浏览 + 质量评分 |
| 规则开发 | 手写 SQL，无版本控制 | 可视化规则树 + SQL 预览 |
| 验证 | 人工跑数 + Excel 核对 | 一键验证 + 留痕可追溯 |
| 发布 | 邮件审批 + 手动配置 | 一键发布 + 自动绑定资产 |
| 复用 | 不知道行内有什么标签 | 标签市场搜索 + 收藏 |

---

## 3. 目标用户与角色

| 角色 | 部门 | 核心需求 | 使用频次 |
|------|------|---------|---------|
| **IT BA** | 科技部/数据平台组 | 翻译业务需求为标签定义，完成技术开发 | 每日使用 |
| **业务分析师** | 市场部/风控部/零售部 | 提出标签需求，使用已有标签做客群分析 | 每周 2-3 次 |
| **数据工程师** | 数据平台组 | 接入数据源、管理数据资产 | 每周使用 |
| **数据治理员** | 数据治理办公室 | 标签合规审核、资产盘点、审计 | 每月使用 |

---

## 4. 用户痛点分析

### 4.1 痛点一：业务需求与技术实现脱节

**问题描述：**

业务人员说"我想找到有留学需求的客户"，IT BA 需要手动翻译成技术规则：
- 客户年龄字段 + 跨境交易频次 > 3 次/月 + 留学相关商户 MCC 码 + 教育类网站访问记录

**影响：**
- 翻译过程中信息丢失（业务说的"有留学需求"和 IT 理解的"跨境交易 > 3 次"可能不一致）
- 每个标签平均 3-5 轮沟通确认
- 高价值需求堆积，低价值标签因沟通成本太高被放弃

**场景还原：**
> 业务："帮我圈一下有留学需求的客户，我要做营销。"
> IT BA："留学需求怎么定义？有海外消费就算？"
> 业务："呃…主要是有子女在国外的吧。"
> IT BA："那年龄要在 40-55 之间？"
> 业务："差不多。还有最近有跨境汇款的那种。"
> （3 天后）
> IT BA 交付了一个 SQL 脚本，但业务发现圈出来的人很多不是目标客户。

### 4.2 痛点二：标签开发流程割裂

**问题描述：**

开发一个标签需要在多个系统间跳转：

| 环节 | 工具 | 问题 |
|------|------|------|
| 需求记录 | Excel / 邮件 | 版本混乱，需求丢失 |
| 数据查找 | 数据字典 / 问 DBA | 不知道数据在哪，质量如何 |
| 规则开发 | SQL 编辑器 / Python | 无法协作，无版本管理 |
| 验证 | 手动跑数 | 结果不可复现 |
| 发布 | 邮件审批 | 流程不可追踪 |
| 文档 | Word / Confluence | 更新滞后，与实际不一致 |

**影响：**
- 一个标签的上下文分散在 3-4 个系统中
- 新人接手需要花 1-2 天了解背景
- 发布后没有自动通知消费方

### 4.3 痛点三：数据发现困难

**问题描述：**

IT BA 在开发标签时，第一步是找到"对的数据"：

- "客户的教育水平字段在哪个库哪张表？" → 可能需要问 2-3 个人
- "这个字段有多少空值？数据质量怎么样？" → 需要跑 SQL 统计
- "这张表每天更新吗？" → 需要查文档或问 DBA
- "这个字段能用吗？有没有 PII？" → 不确定合规要求

**影响：**
- 数据发现占总开发时间的 40%
- 经常开发到一半发现字段不对，从头再来
- 不知道数据质量问题，上线后才发现不准

**场景还原：**
> IT BA 花了一天找到了 age 字段，开发完标签后业务说："这个年龄不准啊，很多客户年龄是空的。"
> 检查发现：空值率 60%，数据质量太差。

### 4.4 痛点四：标签资产无法复用

**问题描述：**

| 场景 | 问题 |
|------|------|
| 信用卡中心做了一个"高净值客户" | 财富管理部不知道，又做了一个 |
| 有人改了一个标签 | 下游系统不知道，继续用旧数据 |
| 要下线一个表 | 不知道哪些标签依赖这个表 |

**影响：**
- 30% 的标签是重复开发
- 标签变更时没有通知机制
- 数据表下线时风险不可控

### 4.5 痛点五：治理合规压力

**问题描述：**

- **PII 检测**：标签使用了身份证号？违反数据隐私法规
- **审计追溯**：监管要求"过去一年发布了哪些标签"——0.5 人天的手工整理
- **血缘丢失**：标签的原始数据来自哪里？说不清楚
- **审批缺失**：标签直接上线，没有经过数据 owner 和合规审批

### 4.6 痛点六：技能门槛高

**问题描述：**

- 业务部门想做标签分析但不会写 SQL
- 简单的规则（"年龄 > 18"）也需要找 IT 帮忙
- 标签做完后只能给技术团队用，业务无法自助

### 4.7 痛点—功能映射矩阵

| 痛点 | 对应的平台功能 | 功能编号 | 状态 |
|------|---------------|---------|------|
| 需求翻译脱节 → 3-5 轮沟通 | RequirementIntake — 输入业务需求自动解析为标签草稿 | FR-01 | ✅ 已实现 |
| 开发流程割裂 → 4 个系统跳转 | 5 步引导式工作台（需求→定义→证据→规则→验证→发布） | FR-02 | ✅ 已实现 |
| 验证无记录 → 改了不知道旧结果 | 每次验证独立记录 + 驳回保留 + 修改标记 stale | FR-03 | ✅ 已实现 |
| 数据发现困难 → 40% 时间在找数据 | EvidenceStep — 已连接资产浏览 + 质量评分 + 脱敏样本 | FR-07 | ✅ 已实现 |
| 标签无法复用 → 30% 重复开发 | 标签市场搜索、浏览、收藏、查重检测 | FR-04 | ✅ 已实现 |
| 血缘丢失 → 表下线风险不可控 | TagBinding — 发布时自动绑定标签到证据表/字段 | FR-05 | ✅ 已实现 |
| 数据源接入 → 手动配置 DSN | 数据连接管理 + 扫描表结构 | FR-06 | ✅ 已实现 |
| 客群圈选 → 手工 SQL | 标签组合圈选 + 画像分析 + 画像对比 | FR-08/09 | ✅ 已实现 |
| 治理全景缺失 | DAMA 治理中心 + 资产盘点 + 标注审核 + 审计日志 | FR-10 | ✅ 已实现 |
| AI 智能规则生成 | 规则草案生成（当前为确定性规则，待 LLM 增强） | — | ⚠️ Phase 2 |
| 审批流程缺失 | TagApproval 模型已预留，未启用 | — | ⚠️ Phase 2 |
| 批量开发 | 当前仅支持单标签开发 | — | ⚠️ Phase 3 |
| 标签血缘图 | 当前为文字展示绑定关系，无可视化图 | — | ⚠️ Phase 3 |

---

## 5. 产品愿景

**让标签开发像填写表单一样简单，让标签复用像逛超市一样自然。**

### 5.1 产品定位

银行标签的全生命周期管理平台，覆盖从需求到发布到复用的完整链路。

### 5.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **引导式而非自由式** | 用户不需要理解平台能力结构，只需要跟随步骤完成当前任务 |
| **AI 辅助而非替代** | AI 生成草案，关键操作由用户确认，保持人在环中 |
| **默认可追溯** | 每个标签从需求到证据到规则到验证到发布，全链路留痕 |
| **一次开发、多处复用** | 标签发布后自动进入市场，支持搜索和引用 |
| **先核心后长尾** | 先服务 IT BA 的核心开发流程，再扩展业务自助分析 |

### 5.3 不做什么

- 不替代数据仓库/数据湖
- 不做实时流式计算
- 不做复杂的机器学习模型训练
- 不做 BI 报表/可视化大屏
- 不做数据血缘的自动解析（依赖手动关联）

---

## 6. 战略设计

### 6.1 产品战略画布

#### 6.1.1 愿景

**让银行的数据标签开发从「手艺活」变成「流水线」—— 每个业务需求在 15 分钟内变成可验证、可追溯、可复用的标签资产。**

这意味着：
- **对 IT BA**：不再在 Excel + SQL + 邮件间反复跳转，一个平台完成全过程
- **对业务**：不再等待 IT 排期，通过标签市场自助发现可复用的客群
- **对治理**：不再临检前突击补文档，每个标签天然携带完整血缘

#### 6.1.2 目标市场与用户细分

| 细分市场 | 典型用户 | 核心痛点 | 首要价值 |
|---------|---------|---------|---------|
| **银行数据平台组** | IT BA、数据工程师 | 标签开发流程长、重复劳动多 | 开发效率提升 10 倍 |
| **零售/零售银行部** | 业务分析师、营销人员 | 无法自助做客群分析，依赖 IT | 标签市场 + 圈选分析 |
| **数据治理办公室** | 数据管理员、合规官 | 标签血缘缺失、审计困难 | 全链路追溯 + 治理视图 |
| **信用卡中心/消金** | 数据分析师 | 客群画像需求多、开发排期长 | 快速圈选 + 画像对比 |

**首批目标用户：** 银行数据平台组的 IT BA
**选择理由：** 他们是标签开发的"瓶颈节点"，提升他们的效率直接缩短全链路周期。他们也是最容易理解平台价值的人群。

#### 6.1.3 成本定位

**定位：差异化价值（差异化竞争，非低成本竞争）**

| 维度 | 选择 |
|------|------|
| **定价策略** | 不追求最低价，强调 ROI（单标签成本从 5000 元 → 500 元） |
| **交付方式** | 私有化部署为主（银行数据不外传） |
| **实施成本** | 低——SQLite 开箱即用，PostgreSQL 生产就绪 |

#### 6.1.4 价值主张

| 维度 | 说明 |
|------|------|
| **What before** | IT BA 在 Excel 接需求 → 问 DBA 找数据 → 手写 SQL → 人工验证 → 邮件发布，5-10 天一个标签 |
| **How** | 输入需求 → AI 解析草稿 → 5 步引导工作台 → 一键验证 → 自动发布 + 绑定资产 |
| **What after** | 15 分钟一个标签，每个标签自带血缘、验证记录、市场可查 |
| **替代方案** | 自建 SQL 脚本 + Excel 管理（零成本但效率低；无治理、无复用） |
| | 采购商业数据治理平台（功能全但价格高、实施周期 3-6 个月） |

### 6.2 战略取舍（Trade-offs）

| 我们选择做什么 | 我们选择不做什么 | 战略理由 |
|-------------|----------------|---------|
| 引导式工作流 | 自由式沙盒 | 降低学习成本，用户不需要理解平台结构就能完成开发 |
| 规则型标签先做 | 模型型标签后做 | 银行 80% 的标签需求是规则型（"age > 18"），模型型是长尾 |
| 人工在环验证 | 全自动发布 | 银行合规要求人在环中，错误标签的代价远高于验证成本 |
| 证据手动选择 | 自动推荐证据 | MVP 阶段先让用户理解和信任数据源；Phase 2 加 AI 推荐 |
| 确定性规则草案 | LLM 生成草案 | 确定性草案 100% 可预期；LLM 草案在 Phase 2 作为可选项 |
| 单标签开发 | 批量标签开发 | 先跑通单标签全流程，再扩展批量能力 |
| SQLite 开箱即用 | 仅支持 PostgreSQL | 降低采用门槛——零配置启动，生产环境再切换 PostgreSQL |
| 私有化部署 | SaaS 多租户 | 银行数据不能出域，私有化是进场门票 |

### 6.3 增长策略

#### 6.3.1 增长模型

```
 数据源接入 → 创建第一个标签 → 发布到市场 → 被引用/复用
    ↑                                              │
    └──────────────────────────────────────────────┘
                  数据源越多 → 标签越多 → 市场越大 → 复用越多
```

#### 6.3.2 增长飞轮

| 阶段 | 驱动力 | 关键动作 |
|------|--------|---------|
| **激活** | 首标签体验 | 新用户引导创建第一个标签（≤ 15 分钟） |
| **留存** | 标签市场价值 | 每次开发新标签前先搜索市场 → 发现有现成的 → 形成习惯 |
| **网络效应** | 数据源扩展 | 接入的数据源越多 → 标签可选的证据越多 → 平台粘性越强 |
| **病毒传播** | 业务部门需求 | IT BA 邀请业务使用标签分析 → 业务提出新需求 → 回到开发流程 |

#### 6.3.3 北极星指标

**月活跃标签发布数（Monthly Active Tag Releases）**

为什么是这个指标：
- 覆盖了开发流程的完成率
- 只有开发完成并发布才算"有价值产出"
- 与数据源接入数、市场复用率正相关

**季度 OMTM（One Metric That Matters）：**

| 季度 | OMTM | 理由 |
|------|------|------|
| Phase 1 | 首标签完成时间 ≤ 15 分钟 | 验证核心工作流是否流畅 |
| Phase 2 | 标签市场复用率 ≥ 30% | 验证市场是否产生价值 |
| Phase 3 | 月活跃标签发布数环比增长 50% | 验证增长飞轮是否启动 |

### 6.4 能力建设

| 能力 | 自建/采购/合作 | 优先级 | 说明 |
|------|-------------|--------|------|
| 标签开发工作流 | 自建 | P0 | 核心竞争力 |
| 数据连接/扫描 | 自建（SQLAlchemy 多方言） | P0 | 核心体验 |
| 规则引擎 | 自建（确定性规则） | P0 | MVP 够用 |
| LLM 规则生成 | API 调用（DeepSeek/OpenAI） | P2 | 降低对 LLM 的依赖，先跑通无 LLM 场景 |
| 知识图谱 3D 可视化 | react-force-graph-3d（开源） | P2 | 视觉增强 |
| 图表可视化 | Recharts（开源） | P0 | 轻量级、够用 |

### 6.5 护城河与竞争壁垒

| 壁垒 | 说明 | 可持续性 |
|------|------|---------|
| **流程绑定** | IT BA 的工作习惯绑定到平台工作流——需求、证据、规则、验证、发布全在平台 | 高——迁移成本随使用时间增加 |
| **数据绑定** | 已连接的数据源、已扫描的表结构、已发布的标签资产 | 高——数据越多越难迁移 |
| **验证历史** | 每个标签的验证记录、驳回记录、历史版本 | 中——可导出但格式不可移植 |
| **市场网络效应** | 标签越多 → 市场越有价值 → 更多用户 → 更多标签 | 极高——这是最核心的壁垒 |
| **银行行业知识** | PII 检测、合规规则、银行数据分类体系 | 中——可通过后期积累形成 |

**竞争对手为什么难复制：**

| 竞争来源 | 我们的优势 |
|---------|----------|
| 自建 SQL 脚本 + Excel | 不需要竞争——这是用户当前的痛点，不是竞品 |
| 商业数据治理平台（Informatica/Collibra） | 价格高（百万级）、实施周期长（3-6 个月）、太重 |
| 开源数据目录（Amundsen/DataHub） | 只解决"数据发现"，不解决"标签开发"；需要大量定制 |
| 内部自研 | 每个银行自己开发成本高、维护难、迭代慢 |

### 6.6 关键假设与验证

| 假设 | 验证方法 | 最低通过标准 |
|------|---------|------------|
| IT BA 愿意用引导式工作流替代 SQL 编辑器 | 观察用户是否完成 5 步流程 | 首标签完成率 ≥ 60% |
| 标签市场能减少重复开发 | 统计发布前是否先搜索市场 | 市场查看率 ≥ 50% |
| 用户愿意保存分群进行画像对比 | 统计保存分群后查看对比的比例 | 对比查看率 ≥ 30% |
| 数据源扫描能覆盖 80% 的常见数据库 | 测试 PostgreSQL/MySQL 的连接和扫描 | 扫描成功率 ≥ 90% |
| LLM 不是必须的——确定性规则够用 | 用户是否能完成标签开发而不需要 LLM | 不依赖 LLM 的完成率 ≥ 80% |

---

## 7. 功能需求

### 7.1 P0 — 核心标签开发

#### FR-01 需求分析与标签草稿生成

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为 IT BA，我输入业务需求，系统自动解析出标签草稿和重复标签提示，我确认后创建开发任务 |
| **验收标准** | 1. 输入"识别有留学需求的客户用于跨境营销"→ 系统提取标签名、业务定义、目标实体 2. 检测到市场中已有相似标签（"留学汇款"）→ 显示重复警告 3. 用户修改草稿字段后确认创建 |
| **优先级** | P0 |

#### FR-02 5 步标签开发工作台

| 步骤 | 功能 | 完成条件 |
|------|------|---------|
| 1. 定义标签目标 | 填写名称、编码、分类、业务定义、目标实体、值类型、更新频率、有效期、负责人 | 所有必填字段完整，重复风险已处理 |
| 2. 选择数据证据 | 从已连接数据源中选择表和字段，查看脱敏样本 | 至少选择一项证据，已查看脱敏样本 |
| 3. 配置标签规则 | 可视化规则树 + AND/OR 组合 + SQL 预览 + AI 规则草案 | 至少一条有效规则，字段在证据范围内 |
| 4. 执行样本验证 | 运行验证、查看命中/未命中/边界样本、审核通过/驳回 | 验证运行成功，已通过人工审核 |
| 5. 确认发布 | 只读发布摘要，确认后创建 v1 | publish_ready = true，已确认 |

#### FR-03 验证留痕与驳回回路

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为 IT BA，我运行验证后发现覆盖率太低，我需要驳回验证、修改规则、重新验证，旧的验证记录不能丢失 |
| **验收标准** | 1. 每次验证创建独立记录 2. 驳回后验证记录标记为 rejected，不清除 3. 规则变更后旧验证标记为 stale 4. 只有最新且 approved 的验证可发布 |

### 7.2 P1 — 标签市场与复用

#### FR-04 标签资产市场

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为业务分析师，我在开发新标签之前先搜索市场，看有没有现成的可以用 |
| **验收标准** | 1. 按分类/名称搜索 2. 每个标签卡片展示名称、分类、覆盖率、准确率 3. 点击查看详情：版本历史、绑定资产、验证记录 4. 收藏功能 |
| **优先级** | P1 |

#### FR-05 标签绑定（Tag Binding）

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为数据治理员，我知道每个标签用了哪些表/字段，下线表之前能评估影响 |
| **验收标准** | 1. 发布时自动绑定标签到证据表/字段 2. 可查询某张表上绑定了哪些标签 3. 治理中心展示标签覆盖情况 |

### 7.3 P1 — 数据连接与资产浏览

#### FR-06 数据源管理

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为数据工程师，我接入 PostgreSQL/MySQL 等数据源，系统自动扫描表结构和字段 |
| **验收标准** | 1. 支持 PostgreSQL/MySQL/MaxCompute/BigQuery 2. 测试连接 3. 扫描发现表名、字段名、类型、注释 4. 扫描进度实时反馈 |

#### FR-07 证据资产浏览

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为 IT BA，我在选择数据证据时能看到所有已连接的表、字段、质量评分和脱敏样本 |
| **验收标准** | 1. 按连接/表名搜索 2. 展示行数、质量评分 3. 展开查看字段列表 4. 查看脱敏样本 5. 空值时提示连接数据源 |

### 7.4 P1 — 客群分析与画像

#### FR-08 标签圈选分析

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为业务分析师，我选择已有标签组合圈选目标客群，系统返回命中数量和画像分析 |
| **验收标准** | 1. 支持 AND/OR 多标签组合 2. 返回命中客户数、覆盖率 3. 展示资产分布、年龄分布、风险偏好图表 4. 客户画像卡片（姓名、资产、产品持有、标签详情） 5. 导出 CSV |

#### FR-09 画像对比

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为业务分析师，我保存了两个分群后想对比它们的特征差异 |
| **验收标准** | 1. 保存分群时保存规则 2. 选择两个分群 → 对比条可视化 3. 各维度（规模、年龄、资产、活跃度）对比 4. 洞察摘要 |

### 7.5 P2 — 数据治理

#### FR-10 数据治理中心

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为数据治理员，我需要一个全景视图看数据资产、标签覆盖、标注审核 |
| **验收标准** | 1. DAMA 11 领域评分 2. 标签覆盖率按数据源展示 3. 标注审核列表 + 批量操作 4. 审计日志查询 |

### 7.6 P2 — 辅助功能

#### FR-11 AI 候选标签

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为 IT BA，我希望 AI 帮我分析数据源，自动推荐可以开发哪些标签 |
| **验收标准** | 1. 点击"智能导入"→ "AI 分析数据源" 2. 自动创建候选标签任务 3. 从知识库提取候选标签 |

#### FR-12 知识管理

| 项目 | 说明 |
|------|------|
| **用户故事** | 作为数据管理员，我上传业务文档，AI 提取标签和实体关系 |
| **验收标准** | 1. 上传文档 2. AI 自动分析提取标签 3. 知识图谱可视化 4. 提取的标签可转为开发任务 |

---

## 8. 用户旅程

### 7.1 完整黄金旅程（IT BA 首次使用）

```
Step 1: 进入平台 → 侧边栏点"标签开发"
Step 2: 首页 → 输入"识别有留学需求的客户用于跨境营销"
Step 3: 系统解析草稿、检测到重复标签 → 确认创建
Step 4: 自动进入工作台 → 第 1 步「定义标签目标」 → 填完所有字段并保存
Step 5: 自动推进到第 2 步「选择数据证据」 → 勾选表、选择字段、查看脱敏样本 → 确认保存
Step 6: 自动推进到第 3 步「配置标签规则」 → 生成 AI 规则草案 → 确认并保存
Step 7: 自动推进到第 4 步「执行样本验证」 → 运行验证 → 查看命中样本 → 确认通过
Step 8: 自动推进到第 5 步「确认发布」 → 确认发布 v1
Step 9: 自动跳转到标签市场 → 查看刚发布的标签详情
```

**耗时：≤ 15 分钟**

### 7.2 业务分析用户旅程

```
Step 1: 进入平台 → 点"标签分析"
Step 2: 选择"留学客群挖掘"场景 → 系统自动加载圈选条件
Step 3: 执行圈选 → 查看结果（命中 12,847 人，覆盖率 3.2%）
Step 4: 查看客户画像卡片 → 点击某客户查看详情
Step 5: 保存分群 → 导出 CSV
```

### 7.3 数据工程师旅程

```
Step 1: 进入平台 → 点"数据连接"
Step 2: 新增 PostgreSQL 数据源 → 填写连接信息 → 测试连接 → 保存
Step 3: 点击"扫描表结构" → 等待扫描完成 → 发现 12 张表
Step 4: IT BA 在标签开发中可以直接看到这些表和字段
```

---

## 9. 成功指标

### 8.1 用户采纳指标

| 指标 | 目标 | 衡量方式 |
|------|------|---------|
| 新用户首次完成标签开发时间 | ≤ 15 分钟 | 从注册到首次发布的时间 |
| 周活跃用户 (WAU) | ≥ 核心团队的 80% | 登录日志 |
| 标签开发完成率 | ≥ 70% | 开始开发的标签中有 70% 最终发布 |
| NPS (净推荐值) | ≥ 40 | 季度用户调研 |

### 8.2 效率指标

| 指标 | 当前 | 目标 |
|------|------|------|
| 单标签平均开发周期 | 5-10 天 | 1-2 小时 |
| 数据发现时间 | 0.5-1 天 | ≤ 5 分钟 |
| 标签市场搜索到复用的时间 | 无 | ≤ 2 分钟 |
| 验证执行时间 | 1-2 小时 | ≤ 30 秒 |

### 8.3 质量指标

| 指标 | 目标 | 衡量方式 |
|------|------|---------|
| 标签覆盖率（已打标表/总表数） | ≥ 60% | 治理中心统计 |
| 标签准确率 | ≥ 90% | 验证通过率 |
| 标签 100% 可追溯 | 100% | 每个标签有完整血缘 |
| 重复标签率 | ≤ 5% | 市场查重 + 发布时检测 |

---

## 10. 发布策略

### Phase 1 — MVP（当前版本 ✅）

| 功能 | 状态 |
|------|------|
| 5 步标签开发工作台 | ✅ 已实现 |
| 需求分析 + 重复检测 | ✅ 已实现 |
| 数据连接 + 扫描 | ✅ 已实现 |
| 证据资产浏览 | ✅ 已实现 |
| 规则配置 + SQL 预览 | ✅ 已实现 |
| 样本验证 + 审核 | ✅ 已实现 |
| 发布 + TagBinding | ✅ 已实现 |
| 标签市场浏览 | ✅ 已实现 |
| 标签分析 + 画像 | ✅ 已实现 |
| 画像对比 | ✅ 已实现 |
| 数据治理中心 | ✅ 已实现 |
| Demo/AI 模式切换 | ✅ 已实现 |

### Phase 2 — 增强（下一迭代）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| AI 智能规则生成 | P0 | 用 LLM 根据业务定义生成规则草案 |
| 标签审批流 | P0 | 发布前数据 owner + 合规审批 |
| 标签版本对比 | P1 | v1 vs v2 规则差异可视化 |
| 批量标签开发 | P1 | 一次需求创建多个标签任务 |
| 标签导出/导入 | P1 | 跨环境迁移标签定义 |

### Phase 3 — 扩展

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 自定义标签包 | P1 | 多标签组合发布（留学客群包） |
| 标签血缘图 | P2 | 可视化展示标签→证据→原始表链路 |
| 主动扫描推荐 | P2 | Agent 自动扫描数据源推荐新标签 |
| 定时刷新 | P2 | 标签支持定时自动更新数据 |

---

## 11. 标签开发流程优化设计

### 11.1 当前状态评估

基于代码审计（2026-06-25），当前标签开发工作流已实现核心链路但存在以下不足：

#### 11.1.1 核心链路状态

| 步骤 | 功能完整性 | 错误处理 | 加载状态 | UX 细节 |
|------|-----------|---------|---------|---------|
| 需求分析 | ✅ 完整 | ⚠️ 错误提示在两处渲染 | ❌ 无加载动画 | ⚠️ 无分类选择器、owner 硬编码 |
| 定义标签目标 | ✅ 完整 | ⚠️ 分类加载失败静默吞掉 | ⚠️ 分类加载无 spinner | ⚠️ 无前端表单校验 |
| 选择数据证据 | ✅ 完整 | ❌ 失败后显示 demo 数据 | ✅ 有 CircularProgress | ⚠️ 搜索仅客户端、无分页 |
| 配置标签规则 | ✅ 完整 | ⚠️ draft 生成失败静默 | ⚠️ 仅按钮 disable | ⚠️ BETWEEN 输入有 bug |
| 样本验证 | ✅ 完整 | ❌ 失败后显示 demo 数据 | ⚠️ progress 不脉冲 | ❌ miss/boundary 样本不展示 |
| 确认发布 | ✅ 完整 | ⚠️ 发布失败 state 提前变更 | ✅ 有 button disable | ⚠️ blocker 消息两处重复 |

#### 11.1.2 关键代码问题

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| 验证结果全是假数据 | 🔴 严重 | `_build_validation_result_with_samples` 用 SHA256 生成伪随机样本，不执行真实数据库查询 |
| 规则草案总是 IS_NULL | 🔴 严重 | `build_rule_draft` 永远生成 `{field: X, operator: "IS_NULL", value: false}` |
| 前端忽略 save 返回的 workflow | 🟡 中等 | 后端每个 step save 都返回 `StepUpdateOut{workflow:{...}}`，但前端 `onMutate` 丢弃了响应，额外调用 `load()` |
| miss/boundary 样本 UI 不存在 | 🟡 中等 | 后端返回了 `miss_samples` 和 `boundary_samples`，但 `ValidationStep` 只渲染 `hit_samples` |
| BlockMessages 两处重复 | 🟢 轻微 | `StepCompletionPanel.tsx` 和 `PublishStep.tsx` 各有一份 17 项的硬编码映射 |
| demo 数据泄漏到生产路径 | 🟡 中等 | `EvidenceStep` 和 `ValidationStep` 的 catch 块用硬编码 demo 数据替代真实错误 |

### 11.2 优化设计

#### 11.2.1 步骤间状态流转优化

**现状问题：** 每次保存步骤后，`onMutate` 调用 `load()` 全量刷新任务数据才能看到步骤推进。前端丢弃了 save API 返回的 `workflow` 对象。

**优化方案：**

```
当前: save → onMutate(operation) → load() → setTask(data) → re-render
优化: save → onMutate(operation) → 从 operation 返回值中提取 workflow → 直接更新 task.workflow → 无额外网络请求
```

**具体设计：**
1. `useTagDevelopmentTask` hook 的 `mutate` 函数接收 operation 的返回值中的 `workflow` 字段，直接合并到当前 `task` 状态
2. 每个 step 组件的 `onMutate` 回调返回 `tagApi.updateXxx()` 的结果，由 hook 提取 `workflow`
3. 仅在首次加载或显式 `reload()` 时调用 `getTaskDetail`
4. **收益：** 保存后推进步骤的响应时间减少一次网络往返（~200ms）

#### 11.2.2 规则配置步骤交互优化

**现状问题：** `BETWEEN` 操作符的值输入使用逗号分隔字符串，双输入框的 onChange 逻辑有竞态 bug；字段选择器 `freeSolo` 允许任意输入绕过证据校验。

**优化方案：**

| 操作符 | 当前交互 | 优化后的交互 |
|--------|---------|------------|
| `=` / `!=` | 文本输入框 | 文本输入框 + 从该字段采样值中推荐选项 |
| `>` / `>=` / `<` / `<=` | 数字输入框 | ✅ 数字输入框（不变） |
| `BETWEEN` | 双文本输入框（逗号拼接） | 两个独立数字输入框，onChange 不再互相覆盖 |
| `IN` | 文本输入框（逗号分隔） | ✅ 文本输入框（不变），增加 placeholder 示例 |
| `IS_NULL` | 文本输入框（值用不到） | 固定显示 "NULL"，禁用输入 |
| `CONTAINS` | 文本输入框 | ✅ 文本输入框（不变） |
| 字段选择 | Autocomplete `freeSolo` | Autocomplete + disabled `freeSolo`，用户只能选择已选证据字段 |

**BETWEEN 输入修复（代码层面）：**

```typescript
// 当前（有 bug）
const from = value.split(',')[0] || '';
const to = value.split(',')[1] || '';
const handleFromChange = (v: string) => onChange(`${v},${to}`);
const handleToChange = (v: string) => onChange(`${from},${v}`);

// 修复后：使用 ref 避免闭包捕获过期值
const valueRef = useRef(value);
valueRef.current = value;
const handleFromChange = (v: string) => {
  const parts = valueRef.current.split(',');
  onChange(`${v},${parts[1] || ''}`);
};
```

#### 11.2.3 验证步骤增强

**现状问题：** 后端返回 hit/miss/boundary 三类样本，但 UI 只展示 hit 样本；验证结果全是伪随机数据；验证失败后无自动导航回规则步骤。

**优化方案：**

```
┌── 验证结果 ──────────────────────────────────────┐
│                                                    │
│  ┌──────┐  ┌──────┐  ┌──────┐                     │
│  │ 命中  │  │ 未命中│  │ 边界  │  ← 三 tab           │
│  │ 1,284 │  │   320 │  │   56 │                     │
│  └──────┘  └──────┘  └──────┘                     │
│                                                    │
│  ┌─ 命中样本（当前 tab）────────────────────────┐  │
│  │  客户 ID  │ 命中原因            │ 评分 │      │  │
│  │  C1001   │ age BETWEEN 12-18   │  92 │      │  │
│  │  C1002   │ city = "北京"        │  88 │      │  │
│  └────────────────────────────────────────────┘  │
│                                                    │
│  [再次验证]  [❌ 验证未通过，返回规则]  [✅ 确认通过] │
│                                                    │
└────────────────────────────────────────────────────┘
```

**具体变更：**
1. 添加 miss/boundary 样本 tab，使用 `Tabs` 切换
2. "验证未通过，返回规则" 按钮自动导航到规则步骤（设置 `currentStep`）
3. 在审核通过前添加加载状态（spinner + 按钮 disable）
4. 后端验证 endpoint 添加 `is_demo_mode()` 判断——demo 模式用伪随机数据，正式模式从数据库查询

#### 11.2.4 发布步骤增强

**现状问题：** blocker 消息两处重复；发布失败后 UI 显示成功状态；无版本历史；无证据字段列表。

**优化方案：**
1. **抽取共享 `BLOCKER_MESSAGES`** 到 `lib/api/tags.ts`，`StepCompletionPanel` 和 `PublishStep` 引用同一来源
2. **修复发布 state 时序：** 先 await API 再 setPublished，如果 API 失败则保持原状态
3. **添加证据字段列表：** 展示每个证据表的已选字段名
4. **添加版本号：** 如果已有 v1，发布步骤显示 "确认发布 v2"（增量版本号）

#### 11.2.5 整体 UI/UX 增强

| 改进项 | 说明 | 优先级 |
|--------|------|--------|
| 步骤完成动画 | 保存成功后步骤图标从 current 变为 completed 时添加 0.3s 过渡动画 | P2 |
| 步骤间导航 | 左侧步骤栏点击 completed 步骤应跳转到该步骤视图，而非仅 reload | P1 |
| 自动保存指示器 | 定义/规则步骤添加 debounce 800ms 自动保存 + "已自动保存" 指示 | P2 |
| 离开确认 | 步骤表单有未保存修改时，离开页面弹出确认对话框 | P2 |
| 分类搜索 | 定义步骤的分类选择器添加搜索功能 | P2 |
| 资产分页 | 证据步骤的资产列表添加后端分页，避免全量加载 | P2 |
| 进度动画 | 验证运行时 LinearProgress 脉冲动画 | P1 |
| 区块提示 | 阻断项步骤栏上显示具体是哪个字段出问题 | P1 |

### 11.3 步骤导航交互优化

#### 11.3.1 当前交互

```
用户点击步骤栏的 "定义标签目标" (completed) → reload()
→ 任务数据刷新 → 仍然是当前步骤的视图
→ 用户困惑：为什么点击了定义但看不到定义表单？
```

#### 11.3.2 优化后交互

```
用户点击步骤栏的 "定义标签目标" (completed) →
  1. 如果该步骤有可编辑内容，切换到该步骤视图（设置 temporaryStep）
  2. 临时步骤显示 "只读模式" banner
  3. 用户可编辑并保存 → 保存后回到真正的 currentStep
  4. 点击 "返回当前步骤" → 回到真正的 currentStep
```

#### 11.3.3 步骤之间的可见性规则

| 步骤状态 | 可查看 | 可编辑 | 说明 |
|---------|--------|--------|------|
| completed | ✅ | ✅ | 用户可以回看和修改已完成步骤 |
| current | ✅ | ✅ | 当前工作步骤 |
| blocked | ❌ | ❌ | 依赖未满足时不能查看/编辑 |
| available（未来用） | ✅ 只读 | ❌ | 依赖已满足但未开始的步骤可预览 |

### 11.4 验证真实性计划

#### 11.4.1 当前状态

**所有验证结果都是伪随机的。** 后端 `_build_validation_result_with_samples` 函数用 SHA256 哈希 + 种子随机数生成命中数、覆盖率、样本数据，没有执行任何数据库查询。

#### 11.4.2 真实验证实施计划

| 阶段 | 说明 | 依赖 |
|------|------|------|
| **Phase 1（当前）** | 确定性 mock 验证，仅供 UI 流程验证 | 无 |
| **Phase 2** | 对已连接的 PostgreSQL 数据源执行真实 SQL 查询，返回真实命中数和样本 | 数据连接 + 表结构扫描就绪 |
| **Phase 3** | 支持多数据源联邦查询（跨 PostgreSQL/MySQL 联合分析） | SQLAlchemy 多方言支持 |

**Phase 2 验证流程设计：**

```
用户点击"执行样本验证"
→ 后端从 draft 读取规则
→ 对证据表执行 SQL 查询：
   SELECT COUNT(*) as hit_count,
          COUNT(*) * 100.0 / (SELECT COUNT(*) FROM evidence_table) as coverage
   FROM evidence_table
   WHERE rule_condition
→ 采样命中/未命中/边界样本
→ 返回结果存储到 TagValidationRun
```

### 11.5 规则草案增强

#### 11.5.1 当前状态

`build_rule_draft(fields)` 永远返回：

```json
[
  {"field": "field1", "operator": "IS_NULL", "value": false, "join": "AND"},
  {"field": "field2", "operator": "IS_NULL", "value": false, "join": "AND"},
  {"field": "field3", "operator": "IS_NULL", "value": false, "join": "AND"}
]
```

这等价于 "field1 IS NOT NULL AND field2 IS NOT NULL AND field3 IS NOT NULL"——几乎没有业务意义。

#### 11.5.2 增强方案

| 方案 | 规则质量 | 依赖 | 优先级 |
|------|---------|------|--------|
| **确定性增强（Phase 1）** | 根据字段类型和数据特征生成合理规则 | 无（纯代码逻辑）| P1 |
| **LLM 增强（Phase 2）** | 根据业务定义 + 字段名称/注释生成智能规则 | LLM 配置 + 字段元数据 | P2 |

**确定性增强规则映射：**

| 字段类型 | 生成规则 | 示例 |
|---------|---------|------|
| boolean | `field = true` | `is_active = true` |
| numeric (age, amount, score) | `field >= threshold` | `age >= 18` |
| enum/category (city, gender) | `field IN (value1, value2)` | `city IN ("北京", "上海")` |
| string (name, desc) | CONTAINS | `field CONTAINS keyword` |
| date/timestamp | `field >= start_date` | `created_at >= '2026-01-01'` |
| 其他 / unknown | 保持 IS_NULL | `field IS NOT NULL` |

阈值从业务定义文本提取：如果业务定义包含"青年" → `age BETWEEN 18 AND 35`；包含"高净值" → `asset_balance >= 6000000`。

---

## 12. 聊天交互与意图识别优化设计

### 12.1 当前状态评估

基于代码审计（2026-06-25），聊天系统存在以下问题：

#### 12.1.1 三套独立聊天 UI

| 聊天组件 | 位置 | 状态 | 上下文 | 后端端点 |
|---------|------|------|--------|---------|
| `ChatPanel.tsx` | 全局浮动面板（非 studio 页面） | 无持久化，页面刷新丢失 | 仅当前路由路径 | `chatApi.send()` |
| `DataAgentStudio` 内嵌聊天 | studio 页面右侧 | 持久化到 DB，支持历史 | 最近 5 条消息 | `POST /studio/chat` |
| `TagDevelopmentTask` 弹出聊天 | 标签开发工作台 | 无持久化，toggle 重置 | 仅任务名称 | `tagApi.chatWithTask()` |

**核心问题：三个聊天互不共享上下文，用户在不同页面需要重复输入相同问题。**

#### 12.1.2 意图识别问题

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **"ReAct Agent" 不是真正的 Agent** | 🔴 严重 | `react_agent.py` 是一个单次 LLM 调用 + 关键字回退，没有工具调用循环、没有中间步骤、没有状态机 |
| **工具从未真正执行** | 🔴 严重 | 系统 prompt 列出了 `search_tags()`、`get_system_overview()` 等工具，但 LLM 只「描述」调用它们，实际从未执行。真实的 `execute_workbench_run` 在另一个路径运行，与 LLM 输出完全脱节 |
| **意图分类是关键字匹配** | 🟡 中等 | `active_view` 路由使用 `in` 做中文子串匹配，"市场"→ 标签市场，"开发"→ 开发工作台。消息"他们在做开发工作"会误触发 |
| **记忆检索是 ILIKE 子串匹配** | 🟡 中等 | 没有向量/语义搜索，没有 embedding，没有排序。`hit_count` 从不更新（代码中 pass 掉了） |
| **自动记忆提取过于宽泛** | 🟡 中等 | 任何含"标签""用户""数据"且超过 10 字符的消息都会保存为记忆条目 |
| **后端无速率限制** | 🟢 轻微 | 无请求频率限制，无消息大小限制 |

#### 12.1.3 聊天交互问题

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **无流式响应** | 🔴 严重 | 三个聊天 UI 都没有 streaming，用户等待 5-15 秒看到完整回复 |
| **`TagDevelopmentTask` 静默降级** | 🟡 中等 | API 失败后静默切换到 `simulateChatReply()` 客户端规则匹配，用户不知是假 AI |
| **无中断/取消按钮** | 🟡 中等 | 发送请求后无法取消 |
| **session 上下文仅 5 条** | 🟡 中等 | 长对话中超过 5 轮的内容被丢弃，无摘要压缩机制 |
| **`msg_count` 硬编码为 2** | 🟢 轻微 | `sync_activity` 的报告消息数始终返回 2，不反映实际聊天轮数 |
| **无 token 计数** | 🟢 轻微 | enhanced_prompt 无限制追加，可能超过 LLM 上下文窗口 |

### 12.2 聊天架构优化设计

#### 12.2.1 统一聊天上下文

**现状：** 三个独立聊天组件，各自管理状态和调用不同的后端端点。

**目标：** 一个统一的聊天上下文层，所有聊天组件共享同一会话状态。

```
┌─────────────────────────────────────────────────────┐
│               ChatContext (统一上下文)                  │
│  ├─ sessionId: 当前会话 ID                           │
│  ├─ messages: Message[] (跨页面持久化)                │
│  ├─ send(msg): Promise<Reply> (统一后端端点)          │
│  ├─ streaming: boolean (流式状态)                    │
│  └─ abort(): void (取消当前请求)                      │
└─────────────────────────────────────────────────────┘
         ▲               ▲                ▲
         │               │                │
  ChatPanel         DataAgentStudio   TagDevelopmentTask
  (全局浮动)        (工作台内嵌)       (工作台弹出)
```

**具体设计：**

1. 新建 `ChatContext`，包装在 `DemoModeProvider` 内部
2. 统一的 `POST /api/chat/send` 端点，取代三个独立的端点
3. 自动注入当前页面上下文（路由、任务 ID、当前步骤）
4. 消息持久化到 `session_studio_messages` 表，`session_id` 按浏览器会话维持
5. 所有聊天组件从同一个 context 读取消息列表

#### 12.2.2 流式响应（SSE）

**现状：** 每个聊天请求是阻塞的 `fetch → await response.json()`。

**目标：** 所有聊天回复通过 Server-Sent Events (SSE) 流式传输。

```
用户发送消息
  ↓
POST /api/chat/send { message, session_id }
  ↓
后端立即返回 200 + Content-Type: text/event-stream
  ↓
data: {"type": "token", "content": "我正在"}
data: {"type": "token", "content": "查询标签市场"}
data: {"type": "action", "view": "market", "params": {...}}
data: {"type": "token", "content": "，找到相关标签"}
data: {"type": "done"}
  ↓
前端逐步渲染 token，到达 done 后结束
```

**前端适配：**
- 使用 `EventSource` 或 `fetch + ReadableStream` 消费 SSE
- 流式 token 逐个追加到聊天气泡中
- `action` 事件触发对应的视图切换（不等待全部完成）
- 用户可随时调用 `abort()` 中断流

#### 12.2.3 真实的 ReAct Agent

**现状：** `react_agent.py` 是单次 LLM 调用 + 关键字回退，不执行任何工具。

**目标：** 实现带工具调用循环的真实 ReAct Agent。

```
用户输入 → 思考 → 调用工具 → 观察结果 → 思考 → ... → 最终回复

工具列表：
├── get_market_tags(query)     → 搜索标签市场
├── get_task_detail(task_id)   → 查询任务状态
├── run_segment_analysis(rules)→ 执行人群圈选
├── get_data_sources()         → 查询数据源列表
├── get_system_overview()      → 系统概览
└── search_memory(query)       → 语义搜索记忆库
```

**实现要点：**
1. 使用 OpenAI 兼容的 `tool_use` / `function_calling` API
2. 最大工具调用轮次：5 轮（防止无限循环）
3. 每轮调用的 `observation` 截断至 2000 token（防止上下文膨胀）
4. 工具执行过程中 SSE 推送 `{"type": "tool_call", "tool": "search_tags", "status": "running"}` 让用户看到 Agent 在思考
5. 不支持需要在用户浏览器中交互的工具（如"打开对话框"）

#### 12.2.4 语义记忆检索

**现状：** `MemoryEntry.key.ilike(f"%{req.message}%")`。

**目标：** 使用 embedding + pgvector 进行语义检索。

| 维度 | 当前 | 目标 |
|------|------|------|
| 检索方式 | ILIKE 子串匹配 | Cosine 相似度向量检索 |
| 排序 | 创建时间 | 相似度分数 + `hit_count` |
| 上下文窗口 | 最后 5 条消息 | 最后 10 条 + 摘要 |
| `hit_count` | 永不更新 | 每次命中 +1 |
| `last_accessed` | 永不更新 | 每次命中更新时间 |

**Phase 1 过渡方案（无需 pgvector）：**

```
1. 关键词提取：用 jieba 分词从用户消息中提取关键词
2. OR 匹配：MemoryEntry.key 含任一关键词即返回
3. 按 hit_count + last_accessed 排序
4. 限制返回前 5 条
```

### 12.3 聊天交互增强

#### 12.3.1 统一的聊天 UI 规范

| 规范 | 说明 |
|------|------|
| **输入框限制** | 最大 2000 字符，超出时显示计数器和截断提示 |
| **发送按钮** | 空输入时禁用；发送中显示 loading spinner，变为"取消"按钮 |
| **消息气泡** | 用户消息右对齐蓝色，AI 消息左对齐白色；AI 消息头部显示模型名称和耗时 |
| **流式指示器** | 打字动画（跳动三个点），同时显示"正在分析..."、"正在查询标签市场..."等状态文字 |
| **错误恢复** | 请求失败显示红色提示 + "重试"按钮，保留用户输入的消息文本 |
| **会话管理** | 右上角"新建会话"按钮；每次新建时清空当前消息但保留历史可回溯 |

#### 12.3.2 上下文感知的快捷建议

不再使用硬编码的 `getContextSuggestions()`，改为：

```
前端发送消息时附带 { context: { path, taskId?, stepId? }}
后端根据路径和任务状态返回 3 条动态快捷建议

示例：
  路径: /tags/development/:taskId, step: evidence
  → 建议: "这个任务需要哪些数据字段？"
  → 建议: "如何判断字段的数据质量？"
  → 建议: "帮我解释脱敏样本的含义"
```

#### 12.3.3 端到端流程示例

```
用户（在标签开发 → 证据步骤）：
  "帮我看看有哪些客户表可以用"

后端 Agent 流程：
  1. 识别意图：search_evidence_assets
  2. 调用 get_data_sources() → 返回已连接的数据库列表
  3. 调用 get_tables("核心银行系统") → 返回表名列表
  4. 推理：用户可能在找客户相关的表
  5. 回复："核心银行系统下有 user_profiles（客户基本信息表，50万行，质量评分 96）...建议选择该表"

前端展示：
  [流式] 我正在查询已连接的数据源...
  [流式] 核心银行系统下有 user_profiles（客户基本信息表...
  [action] { "type": "switch_step", "step": "evidence" }
  [建议] 选择 user_profiles 表
  [建议] 查看脱敏样本
  [建议] 如何判断字段是否合适？
```

### 12.4 实施计划

| 阶段 | 内容 | 优先级 | 预估工作量 |
|------|------|--------|-----------|
| **Phase 1** | 统一 ChatContext + ChatPanel 持久化（localStorage）| P1 | 3 天 |
| **Phase 2** | SSE 流式响应 + 中断取消 | P1 | 5 天 |
| **Phase 3** | 真实 ReAct Agent（tool_use 循环）| P2 | 10 天 |
| **Phase 4** | 语义记忆检索 + `hit_count` 修复 | P2 | 5 天 |
| **Phase 5** | 三聊天下文合并 + 统一后端端点 | P2 | 3 天 |
| **Phase 6** | 动态快捷建议 + token 计数 | P3 | 2 天 |

---

## 13. 附录

### 10.1 术语表

| 术语 | 定义 |
|------|------|
| **标签** | 对客户/账户/交易的分类或评分，如"高净值客户"、"留学需求评分 85 分" |
| **证据** | 标签判定所依赖的数据表或字段 |
| **规则** | 标签的判定逻辑，如"age BETWEEN 12 AND 18" |
| **验证** | 用样本数据验证规则的正确性和覆盖率 |
| **TagBinding** | 标签发布后自动与证据表/字段的关联记录 |
| **覆盖率** | 命中规则的客户数占总客户数的比例 |

### 10.2 关联文档

- [UI Design Spec](../superpowers/specs/2026-06-10-ui-design-spec.md)
- [Tag Development Workflow Design](../superpowers/specs/2026-06-19-tag-development-guided-workflow-design.md)
- [Tag Market Design](../superpowers/specs/2026-06-15-tag-market-discovery-selection-design.md)
- [Tag Analysis Design](../superpowers/specs/2026-06-15-tag-analysis-segmentation-design.md)
- [Implementation Plan](../superpowers/plans/2026-06-19-tag-development-guided-workflow-implementation-plan.md)

### 10.3 数据模型

```sql
-- 核心模型关系
TagDefinition       ← 标签定义
  └── TagVersion     ← 版本控制
       └── TagRelease ← 发布记录
TagDevelopmentTask   ← 开发任务
  ├── TagDraftVersion ← 开发中的草稿
  ├── TagEvidence     ← 数据证据关联
  └── TagValidationRun ← 验证运行记录
TagBinding           ← 标签←→数据关联
```
