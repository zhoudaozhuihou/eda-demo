Frontend rules:
- Use React functional components with hooks only.
- Use Redux Toolkit for global state management.
- Prefer MUI components for UI elements.
- Use TailwindCSS for layout, spacing, and responsive design.
- Avoid inline styles unless strictly necessary.
- Keep components small and composable.
- Separate container logic and presentation when complexity grows.
